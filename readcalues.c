#include <stdio.h>
#include <math.h>

main()
{
    int x,y,z;
    x = y = z = 0;

    printf("Enter values for x:");
    scanf("%d",&x);
    printf("Enter values for y:");
    scanf("%d",&y);
    printf("Enter values for z:");
    scanf("%d",&z);

    printf("The value for x is %d, y is %d and z is %d",x,y,z);
}
