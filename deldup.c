//C Program to Delete duplicate elements from an array

#include <stdio.h>
#include <math.h>
#define MAX 100

main()
{
    int size,i;
    int A[MAX];

    printf("Enter array size:\n");
    scanf("%d",&size);

    printf("Enter array elements:\n");
    for (i=0;i<size;i++)
    scanf("%c",&A[i]);

    printf("\nArrays with unique list: ");


}
